# Libraries

Drop manufacturer **VDMA 66413 (SISTEMA 2.x) XML** libraries here, e.g.
`Pilz_VDMA66413_25-03-31.xml`. If a library ships a `PNG/` icon folder, keep it
alongside the XML. The Verification › Component Library tab loads these.
